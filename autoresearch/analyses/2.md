The last experiment achieved balanced_accuracy=0.724 — well below the 0.78 baseline mentioned for the prior dataset. Several telemetry signals point to undertraining as the dominant issue:

1. **Training stopped extremely early (12 epochs)** with `early_stop/triggered=1`, `best_val_epoch=9`, `final_epoch=12`. The val_loss curve goes 2.627 (epoch 0) → min 2.231 (epoch 8, step 9) → 2.232 (epoch 11). Val loss flattened but barely rose, so this is plateau, not overfit. The training_loss is still falling at the last epoch (last=6.300, min=6.184 at step 9), confirming the model has not converged on its training objective. The current LR=1e-4 with hidden_layer_sizes=(500, 300, 100) appears too cautious — the model is starved of gradient signal and gets cut off before it can keep descending.

2. **Cross-branch error rate is very high (69.35%)** — most errors cross top-level taxonomy boundaries. Top confusions confirm this: Bare substrate → Macroalgae (15% / 319 samples), Rubble → Turf algae (10%), Soft coral → Hard coral (7%), Other invertebrates → Hard coral (7%). The model is making fundamental category-level mistakes, not finer-grained ones — consistent with an under-trained MLP that hasn't had enough useful gradient steps to learn discriminative top-level features.

3. **Calibration shows systematic under-confidence**, especially in the high-confidence bins. Bins 13–20 have avg_confidence ~0.84–0.85 but avg_accuracy 0.95–1.00 (gaps of −0.11 to −0.15). Bin 1 has avg_confidence 0.137 but avg_accuracy 0.281. After sigmoid calibration the predictions sit in a narrow band — symptomatic of a model whose softmax outputs hadn't pulled apart by the time training stopped.

4. **Per-class recall variance is large**: classes with 800–2185 samples reach recall ≥0.91, but several classes with 25–246 samples sit at recall 0.42–0.53 (e.g., 28373b18=0.506 with 79 samples, 748cb267=0.528 with 246 samples). Rare classes have weight_max_min_ratio=1652 already, so the bottleneck is more likely raw learning duration than weight tuning at this stage.

5. **Per-source accuracy spread is wide**: best source (coralnet:3425) hits 0.892 balanced_accuracy while coralnet:1776 / 2055 sit at 0.40–0.48 with cross_branch_error_rate ≥0.97 — domain-shift sources that need more capacity to disambiguate. Combined with the fast halt at 12 epochs, the simplest hypothesis is that LR=1e-4 is too low for this (500, 300, 100) net on this dataset.