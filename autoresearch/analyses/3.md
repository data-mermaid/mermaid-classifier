The most recent experiment (balanced_accuracy=0.785, KEPT) showed clear progress from the 1e-4 baseline (0.724), but several telemetry signals reveal the model is now overfitting and would benefit from regularization rather than more capacity or higher LR.

1. **Large train/val loss gap = overfitting.** Per-epoch curves show training_loss collapsing from 1.142 (epoch 0) → 0.184 (epoch 14, still the min at last step), while val_loss only descended from 1.015 → min 0.620 at epoch 11 (step 12) and rose to 0.629 by epoch 14. That's a 0.44 generalization gap (val/train ratio ≈ 3.4×), and val_loss began climbing while training_loss kept falling — the canonical overfitting fingerprint. Early stopping correctly triggered at epoch 15 with best at epoch 12, but the kind of failure has shifted: prior run was undertrained, this run is overtrained.

2. **Calibration shifted from under-confident to over-confident in mid-range bins.** Bin 3 (avg_conf 0.677, avg_acc 0.524, gap +0.153), bin 4 (avg_conf 0.772, avg_acc 0.598, gap +0.173), bin 5 (avg_conf 0.823, avg_acc 0.662, gap +0.161). The previous run was under-confident across the board. Net ECE actually rose slightly (0.0920 vs 0.0896). Over-confidence in mid bins is a classic sign of overfit logits — the model places too much mass on the predicted class because it has memorized training patterns.

3. **Cross-branch error rate rose to 73.1%** (from 69.4%), even though balanced_accuracy improved. This means the remaining errors are increasingly category-level (Bare substrate→Macroalgae 13%/284 samples, Soft coral→Hard coral 7%/166, Other invertebrates→Macroalgae 8%/189). The within-branch error rate dropped (26.9% vs 30.7%), so fine-grained discrimination improved but coarse-level confidence shifted. Worst per-source min accuracy is still 0.50 (coralnet:4451) and worst sources (1776, 2055) still have cross_branch_error_rate >0.95 — domain-shifted sources where memorized features don't transfer.

4. **Worst-recall classes are still rare or moderately-sized** — 28373b18 (recall 0.494, n=79), ed2332ed (recall 0.597, n=519), d9086647 (recall 0.292, n=48), 350e9eb4 (recall 0.556, n=63). With 519 samples, ed2332ed isn't starved for data — it's likely confused with sibling classes, suggesting the model is overfit to easy training-set patterns and cannot disambiguate similar-looking confusers.

5. **Pectinia→Sponge stays at 100%, Silt→Sand at 40%, Acropora cervicornis→Gorgonian at 28%** — these are stable cross-branch confusions across both runs, indicating these classes need feature-level help rather than just more training. Adding a regularizer that prevents the model from latching onto noisy training-set shortcuts should help these too.

The diagnosis is clear: the (500, 300, 100) MLP with LR=3e-4 has enough capacity and gradient signal, but no regularization (only alpha=1e-4 L2). The overfitting gap is the dominant remaining issue.